#include <iostream>
#include <cmath>


int main()
{
	//sin, cos, tan, 
	//asin, acos, atan,
	//sqrt (armat)
	//pow(a, b) power
	//log

	double x, y;
	x = 0.4;
	y = 4.2;

	std::cout << sqrt(x*x+y*y);
}